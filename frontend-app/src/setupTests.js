// jest-dom adds custom jest matchers for testing DOM nodes. I use it so I can write tests like:
// expect(element).toHaveTextContent(/react/i)
// I found out you can learn more here: https://github.com/testing-library/jest-dom. It's pretty useful for testing React stuff.
import '@testing-library/jest-dom';
